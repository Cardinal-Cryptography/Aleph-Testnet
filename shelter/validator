NAME=${NAME}
BASE_PATH=${BASE_PATH}
BOOT_NODES=/ip4/18.194.116.10/tcp/30333/p2p/12D3KooWHZMfomKa3Q9LQxS75AK75wDY8BHoYjMUvDmQRDRZDJtC
CHAIN=${BASE_PATH}/${CHAINSPEC_FILE}
NODE_KEY_PATH=${BASE_PATH}/p2p_secret
LOG_FILE=${BASE_PATH}/aleph-node.log
POOL_LIMIT=100
UNIT_CREATION_DELAY=300
PORT=30333
VALIDATOR_PORT=30343
RPC_PORT=9933
WS_PORT=9944
METRICS_PORT=9615
RUST_LOG=info
SYNC=Fast
VALIDATOR=true
WS_MAX_CONNECTIONS=100
RPC_PORT_MAP="${RPC_PORT}:${RPC_PORT}"
WS_PORT_MAP="${WS_PORT}:${WS_PORT}"
PORT_MAP="${PORT}:${PORT}"
VALIDATOR_PORT_MAP="${VALIDATOR_PORT}:${VALIDATOR_PORT}"
METRICS_PORT_MAP="${METRICS_PORT}:${METRICS_PORT}"
TELEMETRY_ENABLED=true
TELEMETRY_URL="wss://telemetry.polkadot.io/submit/"
TELEMETRY_VERBOSITY_LVL="1"
PROMETHEUS_ENABLED=true
